<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- API credentials are encrypted with a device-bound Android Keystore key and must never be
         copied to cloud backups or restored onto a different device. -->
    <exclude domain="sharedpref" path="linguafa_ai_preferences.xml"/>
</full-backup-content>
